import 'package:sr_ui/core/core.dart';

class EStatementView extends StatelessWidget {
  const EStatementView({super.key});

  @override
  Widget build(BuildContext context) {
    return ScaffoldBasic(
      titleText: "E-STATEMENTS",
      body: Center(
        child: Text("Account Detail"),
      ),
    );
  }
}
